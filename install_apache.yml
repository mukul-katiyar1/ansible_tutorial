---

- hosts: all
  become: true
  tasks:
  
  - name: Update repository index and install apache package and php support for apache
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes
